import 'package:flutter/material.dart';

class QuickStartSafetyZonesScreen extends StatelessWidget {
  const QuickStartSafetyZonesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('QuickStartSafetyZonesScreen')),
      body: const Center(
        child: Text('Placeholder for QuickStartSafetyZonesScreen'),
      ),
    );
  }
}
