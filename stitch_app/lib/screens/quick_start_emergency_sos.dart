import 'package:flutter/material.dart';

class QuickStartEmergencySosScreen extends StatelessWidget {
  const QuickStartEmergencySosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('QuickStartEmergencySosScreen')),
      body: const Center(
        child: Text('Placeholder for QuickStartEmergencySosScreen'),
      ),
    );
  }
}
