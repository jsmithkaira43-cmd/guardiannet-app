import 'package:flutter/material.dart';

class TeamChatWithMemberListScreen extends StatelessWidget {
  const TeamChatWithMemberListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TeamChatWithMemberListScreen')),
      body: const Center(
        child: Text('Placeholder for TeamChatWithMemberListScreen'),
      ),
    );
  }
}
