import 'package:flutter/material.dart';

class CreateNewZoneScreen extends StatelessWidget {
  const CreateNewZoneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CreateNewZoneScreen')),
      body: const Center(
        child: Text('Placeholder for CreateNewZoneScreen'),
      ),
    );
  }
}
