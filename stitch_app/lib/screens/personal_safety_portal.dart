import 'package:flutter/material.dart';

class PersonalSafetyPortalScreen extends StatelessWidget {
  const PersonalSafetyPortalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PersonalSafetyPortalScreen')),
      body: const Center(
        child: Text('Placeholder for PersonalSafetyPortalScreen'),
      ),
    );
  }
}
