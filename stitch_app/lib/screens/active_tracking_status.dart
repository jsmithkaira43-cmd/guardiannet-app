import 'package:flutter/material.dart';

class ActiveTrackingStatusScreen extends StatelessWidget {
  const ActiveTrackingStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ActiveTrackingStatusScreen')),
      body: const Center(
        child: Text('Placeholder for ActiveTrackingStatusScreen'),
      ),
    );
  }
}
