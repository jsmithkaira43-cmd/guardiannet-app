import 'package:flutter/material.dart';

class QuickStartLiveTrackingScreen extends StatelessWidget {
  const QuickStartLiveTrackingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('QuickStartLiveTrackingScreen')),
      body: const Center(
        child: Text('Placeholder for QuickStartLiveTrackingScreen'),
      ),
    );
  }
}
