import 'package:flutter/material.dart';

class WelcomeToGuardiannetScreen extends StatelessWidget {
  const WelcomeToGuardiannetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WelcomeToGuardiannetScreen')),
      body: const Center(
        child: Text('Placeholder for WelcomeToGuardiannetScreen'),
      ),
    );
  }
}
