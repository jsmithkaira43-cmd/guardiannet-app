import 'package:flutter/material.dart';

class ChatElenaRodriguezScreen extends StatelessWidget {
  const ChatElenaRodriguezScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ChatElenaRodriguezScreen')),
      body: const Center(
        child: Text('Placeholder for ChatElenaRodriguezScreen'),
      ),
    );
  }
}
