import 'package:flutter/material.dart';

class RemoteAlarmControlScreen extends StatelessWidget {
  const RemoteAlarmControlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RemoteAlarmControlScreen')),
      body: const Center(
        child: Text('Placeholder for RemoteAlarmControlScreen'),
      ),
    );
  }
}
